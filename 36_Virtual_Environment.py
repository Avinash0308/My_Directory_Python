import pandas as pd
print(pd.__version__)

# pip freeze : to list all packages installed
# pip freeze > file.txt: to create file with all requirements
# pip install -r file.txt: to install all requirements.